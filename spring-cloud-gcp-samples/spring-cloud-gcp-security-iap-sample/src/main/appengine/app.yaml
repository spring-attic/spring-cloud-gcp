runtime: java
env: flex

handlers:
- url: /.*
  script: this field is required, but ignored

manual_scaling:
  instances: 1

resources:
  memory_gb: 4

env_variables:
  JAVA_OPTS: -Djava.security.egd=file:/dev/./urandom
